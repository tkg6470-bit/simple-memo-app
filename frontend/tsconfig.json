{
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",

    // ▼ 1. React/Vite用の設定 (Reactエラー対策)
    // 必要な型定義を明示的に読み込みます
    "types": ["vite/client", "node", "react", "react-dom"],

    // ▼ 2. バックエンドの型チェックを緩める設定
    "noImplicitAny": false,

    "paths": {
      // バックエンドへのエイリアス
      "@backend/index": ["../backend/src/app.ts"],
      "@backend/*": ["../backend/src/*"],

      // ▼ 3. Hono用の個別パス設定 (Honoエラー対策)
      // これが無いと hono/cors が見つかりません
      "hono": ["./node_modules/hono"],
      "hono/cors": ["./node_modules/hono/dist/cors"],
      "hono/client": ["./node_modules/hono/dist/client"],
      "hono/*": ["./node_modules/hono/dist/*"],
      "@hono/*": ["./node_modules/@hono/*"],

      // ▼ 4. その他のライブラリ用の保険
      "*": ["./node_modules/*"]
    }
  },
  "include": ["src/**/*", "../backend/src/**/*"]
}
