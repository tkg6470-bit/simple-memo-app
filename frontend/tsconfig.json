{
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",

    // ▼ 1. 型チェックを緩める（ビルド優先）
    "noImplicitAny": false,

    // ▼ 2. 必要な型定義をすべて明記
    "types": ["vite/client", "node", "react", "react-dom"],

    "paths": {
      // バックエンドへのエイリアス
      "@backend/*": ["../backend/src/*"],

      // ▼ 3. Honoのパス解決 (ここを強化しました)
      "hono": ["./node_modules/hono"],

      // フォルダ(index.d.ts) と ファイル(.d.ts) の両方を探すように指定
      "hono/*": [
        "./node_modules/hono/dist/*",
        "./node_modules/hono/dist/*.d.ts"
      ],

      // ▼ 4. その他のライブラリ用のバックアップ
      "*": ["./node_modules/*"]
    }
  },
  "include": ["src/**/*", "../backend/src/**/*"]
}
