// DB接続設定
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// クライアント生成設定
generator client {
  provider      = "prisma-client-js"
  // ↓↓↓ ここを追加しました！ ↓↓↓
  // Mac(arm64)やDocker(linux)など、複数の環境で動くように指定します
  binaryTargets = ["native", "linux-musl-openssl-3.0.x", "linux-arm64-openssl-1.1.x", "debian-openssl-1.1.x", "debian-openssl-3.0.x"]
}

// 📝 Memoモデルの定義
model Memo {
  id         Int      @id @default(autoincrement()) // SERIAL PRIMARY KEY
  title      String   @db.VarChar(255)              // VARCHAR(255)
  content    String   @db.Text                      // TEXT
  created_at DateTime @default(now())               // DEFAULT CURRENT_TIMESTAMP
  updated_at DateTime @default(now()) @updatedAt    // 自動更新

  // 実際のテーブル名を 'memos' に指定
  @@map("memos")
}